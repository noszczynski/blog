import React from "react"
// import Img from "gatsby-image"

const Image = ({ id, ...props }) => {
  // return data ? (
  //   <Img fluid={data} {...props} />
  // ) : (
  //   <div>Picture not found</div>
  // )

  return <div>Picture not found</div>
}

export default Image
